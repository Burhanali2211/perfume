# Supabase Configuration
# Copy this file to .env and fill in your actual values

# Supabase Project URL
# Get this from your Supabase project settings → API → Project URL
VITE_SUPABASE_URL=your_supabase_project_url

# Supabase Anon Key
# Get this from your Supabase project settings → API → Project API keys → anon public
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: Environment
VITE_APP_ENV=development

# Optional: App Configuration
VITE_APP_NAME="Aura E-Commerce"
VITE_APP_VERSION=2.0.0

# Payment Configuration (for future implementation)
# VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
# VITE_PAYPAL_CLIENT_ID=your_paypal_client_id

# Analytics (for future implementation)
# VITE_GOOGLE_ANALYTICS_ID=your_ga_id

# Database Configuration Notes:
# 1. Make sure you've applied the complete schema from supabase_new_complete_schema.sql
# 2. Verify that all tables, functions, and policies are created
# 3. Test the connection by running: SELECT * FROM public.profiles LIMIT 1;
# 4. Create your first admin user by running: SELECT public.make_user_admin('your-email@example.com');

# Email Service (for future implementation)
# VITE_EMAILJS_SERVICE_ID=your_emailjs_service_id
# VITE_EMAILJS_TEMPLATE_ID=your_emailjs_template_id
# VITE_EMAILJS_PUBLIC_KEY=your_emailjs_public_key
